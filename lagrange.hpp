#include "lagrange.h"

template <typename Type>

ModelHandler::lagrange<Type>::lagrange(Model<Type> F)
{
    this-> F = F;
}

template <typename Type>
Type ModelHandler::lagrange<Type>::encontrarFuncao(Type X,Type Y)
{
}

